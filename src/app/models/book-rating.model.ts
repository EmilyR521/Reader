/**
 * Book Rating Enum
 * Represents the rating a user can give to a book
 */
export enum BookRating {
  None = 'none',
  Positive = 'positive',  // Thumbs up
  Negative = 'negative',  // Thumbs down
  Favourite = 'favourite' // Heart
}
